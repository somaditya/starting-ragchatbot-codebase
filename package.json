{
  "name": "course-materials-rag-frontend-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend tooling (Prettier) for the Course Materials RAG chatbot. The frontend itself is plain HTML/CSS/JS — this file exists only to pin formatting tool versions.",
  "scripts": {
    "format": "prettier --write \"frontend/**/*.{js,html,css,json,md}\"",
    "format:check": "prettier --check \"frontend/**/*.{js,html,css,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
